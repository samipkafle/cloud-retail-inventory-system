// GreenLeaf Inventory System application entry point.
// Starts the application after the HTML page has fully loaded.

import { initialise } from "./js/app.js";

document.addEventListener("DOMContentLoaded", initialise);
